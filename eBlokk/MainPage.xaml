<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="eBlokk.MainPage"
             Title="">

    <Grid>
        <!-- Grid sorok definiálása -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- Tartalom -->
            <RowDefinition Height="60" />
            <!-- Lábléc -->
        </Grid.RowDefinitions>

        <!-- Címke, amely a bejelentkezett felhasználó nevét jeleníti meg -->
        <Label Text="{Binding Username, StringFormat='Szia, {0}!'}"
               FontSize="24"
               HorizontalOptions="Start"
               VerticalOptions="Start"
               Padding="20, 20, 0, 0"
               Style="{StaticResource TitleStyle}" />

        <!-- Tartalom a felső sorban -->
        <ScrollView Grid.Row="0">
            <VerticalStackLayout
                Padding="30,0"
                Spacing="25">
                <Image
                    Source="C:\Users\diak\Documents\HL\Projekt\VS\MauiApp1\MauiApp1\Resources\Images\blokk.png"
                    HeightRequest="185"
                    Aspect="AspectFit"
                    SemanticProperties.Description="dot net bot in a race car number eight" />

                <Label
                    Text="eBlokk App"
                    Style="{StaticResource Headline}"
                    SemanticProperties.HeadingLevel="Level1" />

                <Label
                    Text="Üdvözellek a papírmentes nyugták világában!"
                    Style="{StaticResource SubHeadline}"
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

                <!-- A QR kód gomb -->
                <Button Text="QR kód" HorizontalOptions="Fill" Clicked="Button_Clicked_1"/>
            </VerticalStackLayout>
        </ScrollView>

        <!-- Lábjegyzet a második sorban -->
        <Grid Grid.Row="1" BackgroundColor="#3E4A89" VerticalOptions="End" Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Text="Kezdőlap" Grid.Column="0" />
            <Button Text="Blokkjaim" Grid.Column="1" Clicked="OnMyReceiptsPageClicked" />
            <Button Text="Profil" Grid.Column="2" Clicked="ProfilePageClicked"/>
        </Grid>
    </Grid>

</ContentPage>
